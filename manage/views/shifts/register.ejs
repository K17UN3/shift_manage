<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>シフト管理・登録</title>
    <style>
        body { font-family: sans-serif; background: #f0f2f5; margin: 0; padding: 10px; box-sizing: border-box; }
        
        .container { 
            width: 100%;
            max-width: 900px; 
            margin: 0 auto; 
            background: white; 
            padding: 15px; 
            border-radius: 12px; 
            box-sizing: border-box;
        }

        .header-area { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; }
        h1 { font-size: 1.1rem; border-left: 5px solid #007bff; padding-left: 10px; margin: 0; }

        /* 登録フォーム */
        .form-section { background: #f8f9fa; padding: 15px; border-radius: 10px; border: 1px solid #eef0f2; margin-bottom: 25px; }
        .form-grid { display: grid; grid-template-columns: 1fr; gap: 12px; margin-bottom: 12px; }

        @media (min-width: 768px) {
            .form-grid { grid-template-columns: 1fr 1fr 1.5fr; }
            .container { padding: 30px; }
        }

        label { display: block; font-weight: bold; font-size: 0.8rem; margin-bottom: 4px; color: #555; }
        input, select { width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 6px; font-size: 16px; box-sizing: border-box; }

        .btn-submit { 
            width: 100%; background: #007bff; color: white; border: none; padding: 12px; 
            border-radius: 6px; font-weight: bold; cursor: pointer; font-size: 1rem; 
        }

        /* --- シフト一覧（スマホでカード化） --- */
        .shift-list-container { margin-top: 20px; }
        
        table { width: 100%; border-collapse: collapse; }
        
        /* PC用：通常のテーブル表示 */
        @media (min-width: 600px) {
            th, td { padding: 12px; text-align: left; border-bottom: 1px solid #eee; }
            th { background: #f8f9fa; font-size: 0.85rem; }
            tr:nth-child(even) { background-color: #fafafa; } /* 行ごとの背景色 */
        }

        /* スマホ用：カード形式に変換 */
        @media (max-width: 599px) {
            thead { display: none; } /* 見出しを隠す */
            
            tr { 
                display: block; 
                background: #fdfdfd; 
                border: 1px solid #eee; 
                border-radius: 10px; 
                margin-bottom: 12px; 
                padding: 10px;
                position: relative;
                box-shadow: 0 2px 5px rgba(0,0,0,0.02);
            }
            
            /* 1行ごとの背景色を交互に変える場合 */
            tr:nth-child(even) { background-color: #f7f9fc; }

            td { 
                display: flex; 
                justify-content: space-between; 
                align-items: center;
                padding: 6px 0; 
                border: none;
                font-size: 0.9rem;
            }

            /* 各項目の左側にラベルを表示させる設定 */
            td::before {
                content: attr(data-label);
                font-weight: bold;
                color: #888;
                font-size: 0.75rem;
            }

            .time-badge { font-size: 0.9rem; font-weight: bold; color: #007bff; }
        }

        .time-badge { background: #e7f5ff; color: #1971c2; padding: 4px 8px; border-radius: 6px; }
        .btn-delete { color: #e03131; background: none; border: none; text-decoration: underline; cursor: pointer; padding: 5px; }
    </style>
</head>
<body>
<div class="container">
    <div class="header-area">
        <h1><%= isAdmin ? '管理者用シフト管理' : 'シフト登録' %></h1>
        <a href="/shifts/home" style="text-decoration:none; color:#007bff; font-weight:bold;">← ホームに戻る</a>
    </div>

    <% if (error) { %>
        <div class="error-msg"><strong>⚠️ エラー:</strong> <%= error %></div>
    <% } %>

    <div class="form-section">
        <form action="/shifts/register" method="POST">
            <div class="form-grid">
                <div>
                    <label>スタッフ</label>
                    <% if (isAdmin) { %>
                        <select name="employee_id" required>
                            <% if (username !== 'admin') { %>
                                <option value="<%= userId %>">自分 (<%= username %>)</option>
                            <% } %>

                            <% allUsers.forEach(user => { %>
                                <% if (user.id != userId && user.username !== 'admin') { %>
                                    <option value="<%= user.id %>"><%= user.username %></option>
                                <% } %>
                            <% }); %>
                        </select>
                    <% } else { %>
                        <input type="text" value="<%= username %>" disabled>
                        <input type="hidden" name="employee_id" value="<%= userId %>">
                    <% } %>
                </div>
                <div>
                    <label>日付</label>
                    <input type="date" name="date" required value="<%= moment().format('YYYY-MM-DD') %>">
                </div>
                <div style="display: flex; gap: 10px;">
                    <div style="flex:1">
                        <label>開始</label>
                        <input type="time" name="start" required value="09:00">
                    </div>
                    <div style="flex:1">
                        <label>終了</label>
                        <input type="time" name="end" required value="17:00">
                    </div>
                </div>
            </div>
            <button type="submit" class="btn-submit">登録する</button>
        </form>
    </div>

    <h2>📅 今月のシフト一覧 (合計: <%= currentHours %>h)</h2>
    <table class="shift-table">
        <thead>
            <tr>
                <th>スタッフ</th>
                <th>日付</th>
                <th>時間</th>
                <% if (isAdmin) { %><th>操作</th><% } %>
            </tr>
        </thead>
        <tbody>
            <% registeredShifts.forEach(shift => { %>
                <tr>
                    <td>
                        <strong><%= shift.username %></strong>
                        <br><small style="color: #666;"><%= shift.user_role %></small>
                    </td>
                    <td><%= shift.shift_date %></td>
                    <td><span class="time-badge"><%= shift.start_time %> - <%= shift.end_time %></span></td>
                    <% if (isAdmin) { %>
                        <td>
                            <form action="/shifts/delete/<%= shift.id %>" method="POST" style="margin:0;" onsubmit="return confirm('削除しますか？');">
                                <button type="submit" style="background:none; border:none; color:#e03131; cursor:pointer; text-decoration:underline; font-size:0.8rem;">削除</button>
                            </form>
                        </td>
                    <% } %>
                </tr>
            <% }); %>
        </tbody>
    </table>
</div>
</body>
</html>
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/home.css">
    <title>ホーム</title>
</head>
<body class="page-home">
<div class="container">
    <div class="header">
        <h1>こんにちは、<%= username %> さん</h1>
        <a href="/logout" class="btn-logout">ログアウト</a>
    </div>

    <div class="today-info">
        今日は <%= moment().format('YYYY年 M月 D日 (ddd)') %> です
    </div>

    <div class="menu-list">
        <a href="/shifts/register" class="menu-item">
            シフトを提出・確認する
        </a>

        <a href="/shifts/view" class="menu-item">
            月間全体表を見る
        </a>

        <a href="/shifts/year" class="menu-item">
            年間全体表を見る
        </a>

        <% if (isAdmin) { %>
            <a href="/admin/users" class="menu-item" style="background: #fff5f5; border-color: #ffc9c9; color: #e03131;">
                従業員アカウント管理
            </a>
        <% } %>
    </div>

    <% if (typeof isAdmin !== 'undefined' && isAdmin) { %>
        <div style="margin-top: 30px; font-size: 0.75rem; color: #aaa;">
            管理者モードでログイン中
        </div>
    <% } %>
</div>
</body>
</html>
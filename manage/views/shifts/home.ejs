<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ホーム</title>
    <style>
        /* 基本スタイル：余計な余白を削り、中央寄せに */
        body { 
            font-family: sans-serif; 
            background: #f8f9fa; 
            margin: 0; 
            padding: 20px 10px; /* 左右に少しだけ余白を持たせる */
            color: #333; 
            box-sizing: border-box;
        }

        /* 画面幅の調整：スマホでは画面の約95%、PCでは最大400px */
        .container { 
            width: 95%; 
            max-width: 400px; 
            margin: 0 auto; 
            text-align: center; 
            box-sizing: border-box;
        }

        /* ヘッダー：名前とログアウト */
        .header { 
            display: flex; 
            justify-content: space-between; 
            align-items: center; 
            margin-bottom: 30px; 
        }
        h1 { font-size: 1.1rem; margin: 0; }
        .btn-logout { font-size: 0.8rem; color: #999; text-decoration: none; }

        /* 今日の日付表示 */
        .today-info { font-size: 0.95rem; color: #666; margin-bottom: 25px; }

        /* メニューボタンのリスト：隙間を維持 */
        .menu-list { display: flex; flex-direction: column; gap: 15px; }

        /* メニュー項目：見た目はそのまま、タップしやすく */
        .menu-item {
            background: white;
            padding: 22px 15px; /* 上下を少し厚くして押しやすく */
            border-radius: 12px;
            border: 1px solid #ddd;
            text-decoration: none;
            color: #333;
            font-weight: bold;
            font-size: 1rem;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 2px 6px rgba(0,0,0,0.05);
            transition: background 0.2s, transform 0.1s;
        }
        
        /* 押し心地を良くする設定 */
        .menu-item:active {
            background: #f1f3f5;
            transform: scale(0.98);
        }

        /* 管理者用ボタン：色はそのまま維持 */
        .admin-item {
            background: #fff5f5 !important;
            border-color: #ffc9c9 !important;
            color: #e03131 !important;
        }
    </style>
</head>
<body>
<div class="container">
    <div class="header">
        <h1>こんにちは、<%= username %> さん</h1>
        <a href="/logout" class="btn-logout">ログアウト</a>
    </div>

    <div class="today-info">
        今日は <%= moment().format('YYYY年 M月 D日 (ddd)') %> です
    </div>

    <div class="menu-list">
        <a href="/shifts/register" class="menu-item">
            シフトを提出・確認する
        </a>

        <a href="/shifts/view" class="menu-item">
            月間全体表を見る
        </a>

        <a href="/shifts/year" class="menu-item">
            年間全体表を見る
        </a>

        <% if (isAdmin) { %>
            <a href="/admin/users" class="menu-item" style="background: #fff5f5; border-color: #ffc9c9; color: #e03131;">
                従業員アカウント管理
            </a>
        <% } %>
    </div>

    <% if (typeof isAdmin !== 'undefined' && isAdmin) { %>
        <div style="margin-top: 30px; font-size: 0.75rem; color: #aaa;">
            管理者モードでログイン中
        </div>
    <% } %>
</div>
</body>
</html>